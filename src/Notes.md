# clone private branch to public
# git remote add public https://github.com/YOUR-USERNAME/public-repo.git
# git push public local-branch:remote-branch

# Force-push needed if branch already exists but is empty
# git push -f public local-branch:remote-branch


# Github deployment for static pages
# ng build --configuration production --base-href ./ --deploy-url ./
# Deploy
# npx angular-cli-ghpages --dir=dist/designbuilderx/browser --repo=https://github.com/designbuilderx/testing-design.git

